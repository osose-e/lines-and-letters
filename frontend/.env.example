# API URL for the Flask backend (Cloud Run). Used at build time.
# Copy to .env and set to your deployed Cloud Run URL, e.g.:
# REACT_APP_API_URL=https://flask-fire-xxxxx-uc.a.run.app
REACT_APP_API_URL=
